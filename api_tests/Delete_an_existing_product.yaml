// Test generated by RoostGPT for test Amicorp-Integration using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}/api"
  phases:
    - duration: 60
      arrivalRate: 20
  variables:
    productId: 1

scenarios:
  - name: "Delete an existing product"
    flow:
      - log: "Given there is an existing product with ID 1"
      - get:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
      - log: "When the client sends a DELETE request to /products/1"
      - delete:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 200
      - log: "Then the response status code should be 200"
      - think: 1
      - get:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 404
      - log: "And the product with ID 1 should no longer exist"
